$(function () {

    if (!$('.demo').length) return;

    (function simple() {
        var flipsnap = Flipsnap('#demo-simple .flipsnap');

        flipsnap.toNext();
        flipsnap.toNext();
        flipsnap.toNext();
        flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
        // flipsnap.toNext();
    })();
});